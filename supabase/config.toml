project_id = "ykrgbwvrvocprjvpqxej"

[auth.email]
# Reduce one-time password (OTP) lifetime to the recommended threshold (<= 1 hour)
# to limit the window in which an intercepted code can be reused.
otp_expiry = 3600
